# Snippets

Short, runnable examples for the concepts in `../notes/`.

Each file is standalone — no imports across snippets, no hidden setup.
Run any of them directly:

```bash
python <filename>.py
```
